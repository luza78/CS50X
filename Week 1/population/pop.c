#include <cs50.h>
#include <stdio.h>

    // x pop start
    // y pop end
    // z year


int main(void)
{
    int x = get_int("pop start: ");
    int y = get_int("pop end: ");
    int g;
    int z = 0;
    for(g = 0; g < y; z++)
{
        (g = x + (x / 3) - (x / 4));
}

  printf("%i\n",z);
  return 0;
}